{
  "name": "firefox-addon-notion-favicon",
  "version": "1.0.0",
  "description": "Always display the default Notion favicon instead of switching the favicon to the icon of the currently activated page.",
  "scripts": {
    "lint": "zip -r /tmp/addon-lint.zip manifest.json src/ && addons-linter /tmp/addon-lint.zip",
    "test": "jest --coverage"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/just-barcodes/firefox-addon-notion-favicon.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/just-barcodes/firefox-addon-notion-favicon/issues"
  },
  "homepage": "https://addons.mozilla.org/en-US/firefox/addon/static-notion-favicon/",
  "devDependencies": {
    "addons-linter": "^10.3.0",
    "jest": "^30.3.0",
    "jest-environment-jsdom": "^30.3.0"
  }
}
